apiVersion: apps/v1
kind: Deployment
metadata:
  name: utility-app
  namespace: staging
  labels:
    app: utility-app
    environment: staging
spec:
  replicas: 3
  selector:
    matchLabels:
      app: utility-app
  template:
    metadata:
      labels:
        app: utility-app
        environment: staging
    spec:
      containers:
      - name: utility-container
        image: noelav07/utility:3.0  #staging image
        ports:
        - containerPort: 8090
        env:
        - name: ENVIRONMENT
          value: "staging"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
